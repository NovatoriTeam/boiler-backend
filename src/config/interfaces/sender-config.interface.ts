export interface SenderConfigInterface {
  readonly apiKey: string;
  readonly apiUrl: string;
  readonly twilioSecretId: string;
  readonly twilioToken: string;
  twilioVerificationServiceId: string;
}
